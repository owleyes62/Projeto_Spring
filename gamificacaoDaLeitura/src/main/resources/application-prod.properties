# 🔷 Identificação do app
spring.application.name=gamificacaoDaLeitura

# 🔷 Servidor (porta dinâmica do Render + contexto do app)
server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.context-path=/yomu

# 🔶 Datasource (Supabase / Postgres) -> SEM defaults aqui; tudo via env!
spring.datasource.url=jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.trtqlarsnsdinqkqgwpk
spring.datasource.password=YomuSupa#62
spring.datasource.driver-class-name=org.postgresql.Driver

# 🔶 HikariCP (afinado para PgBouncer/pooling)
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.auto-commit=true
# Recomendado para PgBouncer:
spring.datasource.hikari.data-source-properties.preferQueryMode=simple
spring.datasource.hikari.data-source-properties.prepareThreshold=0

# 🔶 JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=true

# 🔶 Logs (produção mais silenciosa)
logging.level.root=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.com.zaxxer.hikari=INFO
logging.level.br.com.yomu.gamificacaoDaLeitura=INFO

# 🔶 Swagger / OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.enabled=true
